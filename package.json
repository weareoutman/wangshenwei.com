{
  "name": "wangshenwei.com",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo 'nothing to test'",
    "deploy": "gatsby build && gh-pages --no-history -d public -b deploy -r https://$GH_TOKEN@github.com/weareoutman/wangshenwei.com.git",
    "preview": "gatsby build && gh-pages --no-history -d public -b preview -r https://$GH_TOKEN@github.com/weareoutman/wangshenwei.com.git"
  },
  "dependencies": {
    "@theme-ui/preset-deep": "^0.3.0",
    "@theme-ui/preset-swiss": "^0.3.0",
    "gatsby": "^2.24.10",
    "gatsby-plugin-google-analytics": "^2.3.13",
    "gatsby-plugin-manifest": "^2.4.20",
    "gatsby-plugin-nprogress": "^2.3.10",
    "gatsby-theme-blog": "^2.0.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "sharp": "^0.25.4"
  }
}
